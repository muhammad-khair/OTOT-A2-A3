apiVersion: v1
kind: Service
metadata:
  labels:
    app: service
  name: service
spec:
  selector:
    app: backend
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 80
      protocol: TCP
